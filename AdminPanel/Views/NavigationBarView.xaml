<UserControl x:Class="AdminPanel.Views.NavigationBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdminPanel.Views"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" Height="40" HorizontalAlignment="Right" VerticalAlignment="Top">
        <Button FontSize="20" Command="{Binding NavigateDriverPricingCommand}">Pricing</Button>
        <Button FontSize="20" Command="{Binding NavigateDriverListCommand}">Drivers</Button>
        <Button FontSize="20" Command="{Binding NavigateAddDriverCommand}">Add Driver</Button>
    </StackPanel>
</UserControl>
